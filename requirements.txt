# ─────────────────────────────────────────────────────────
# ASVault v2 — Requirements
# ─────────────────────────────────────────────────────────

# GUI
customtkinter==5.2.2
pillow>=10.0.0

# ── Local GGUF model support ──────────────────────────────
# For CPU only:
#   pip install llama-cpp-python
#
# For GPU (NVIDIA CUDA) — recommended for RTX cards:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu122
#
# Uncomment the line below for CPU-only install:
# llama-cpp-python==0.2.90

# ── Online API clients (optional) ────────────────────────
openai>=1.30.0
anthropic>=0.25.0
google-generativeai>=0.5.0

# ── Utilities ────────────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0

# ── Build (only needed to make .exe) ─────────────────────
pyinstaller>=6.5.0
